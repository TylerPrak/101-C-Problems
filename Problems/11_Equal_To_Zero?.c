#include <stdio.h>

int		main(int argc, char *argv[]) {

	int num;

	scanf("%d", &num);
	if (num == 0)
		printf("The number is equal to zero.");
	else
		printf("The number is not equal to zero.");

	printf("\n");
	return 0;
}
